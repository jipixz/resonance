<?xml version="1.0" encoding="utf-8"?>
<!--
    Shown for the instant between the widget being placed and Glance drawing
    over it. Mandatory: without an initialLayout the system has nothing to put
    on screen at all, and a widget in that state neither takes taps nor offers
    resize handles.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_preview_background">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/app_name"
        android:textSize="14sp" />
</FrameLayout>
